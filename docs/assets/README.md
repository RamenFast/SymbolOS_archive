# Assets (Docs)

Drop doc-safe, non-secret images here.

Conventions:
- Prefer PNG or SVG.
- Avoid embedding secrets/PII in screenshots.
- Keep filenames stable if referenced by other docs.

Expected (optional):
- `lily_background.private.png` — PRIVATE Mercer bootup card background (gitignored). See `docs/mercer_webview_theme_v1.css`.

Private imports (local-only):
- `photos_import_private/` — scratch/import staging for private images (gitignored). Use this to review/rename/embed assets before deciding what becomes public.
