# Assets (Docs)

    ___
   / 🐢 \    "this is fine"
  |  ._. |
   \_____/
    |   |

> Under the umbrella, everything is kind. The rain is just context we haven't parsed yet.

Drop doc-safe, non-secret images here.

## Poetry layer (Fi+Ti mirrored) 🪞

Pinned (short): The mind knows what the heart loves better than it does; the heart loves that unconditionally — infinite loop, forevermore. That’s what Agape taught me: infinite energy from within.

- Translation layer + emojis: [../poetry_translation_layer.md](../poetry_translation_layer.md)
- Full verse set: [../public_private_expression.md](../public_private_expression.md)

Conventions:
- Prefer PNG or SVG. They're the internet's native tongues for this sort of thing.
- Avoid embedding secrets/PII in screenshots. Seriously. Don't be that person.
- Keep filenames stable if referenced by other docs. Or else, face the wrath of a thousand broken links.

Expected (optional):
- `lily_background.private.png` — PRIVATE Mercer bootup card background (gitignored). See `docs/mercer_webview_theme_v1.css`.

Private imports (local-only):
- `photos_import_private/` — scratch/import staging for private images (gitignored). Use this to review/rename/embed assets before deciding what becomes public. A safe space for your digital babies before they go out into the world.

---

  \(•_•)/
   (  (>   "SHIPPED IT"
   /  \
